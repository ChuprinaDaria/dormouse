"""Shared helpers for the dataset/training pipeline scripts.

Not part of the dormouse package — scripts and tests import it by adding
scripts/ to sys.path (same pattern as tests/test_import.py).
"""
